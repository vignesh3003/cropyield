// Set BASE_URL to the address where your Flask API is reachable from the device
// e.g. for Android emulator use 10.0.2.2:5001, for iOS simulator use localhost:5001,
// for a real device use your machine's LAN IP like 192.168.1.10:5001

export const BASE_URL = "http://10.0.2.2:5001";
